[package]
name = "pulseview-wasm-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "=0.2.100"

[profile.release]
lto = true
codegen-units = 1
opt-level = "s"
strip = true
